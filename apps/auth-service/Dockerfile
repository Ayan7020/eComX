# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @e-com-x/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t e-com-x-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system e-com-x-auth-service && \
          adduser --system -G e-com-x-auth-service e-com-x-auth-service

COPY apps/auth-service/dist e-com-x-auth-service/
COPY apps/auth-service/package.json e-com-x-auth-service/
RUN chown -R e-com-x-auth-service:e-com-x-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix e-com-x-auth-service --omit=dev -f install

CMD [ "node", "e-com-x-auth-service" ]
